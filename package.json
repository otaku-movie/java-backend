{
  "name": "java-backend",
  "private": true,
  "description": "Otaku Movie Java 后端（commitlint / husky 仅用于 Git 提交规范）",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "husky": "^9.1.7"
  }
}
